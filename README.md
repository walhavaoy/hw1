# hw1 - Hello World Test App

## Project Structure

```
src/
├── index.html   # Main HTML page
├── styles.css   # Dark theme styles
└── app.js       # Time display and greeting logic
```

Open `src/index.html` in a browser to run the app.
